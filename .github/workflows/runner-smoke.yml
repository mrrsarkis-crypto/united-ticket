name: Runner Smoke Test

on:
  workflow_dispatch:

jobs:
  smoke:
    runs-on: ubuntu-24.04
    steps:
      - name: Verify hosted runner
        run: |
          echo "runner=${RUNNER_NAME}"
          node --version
          npm --version
